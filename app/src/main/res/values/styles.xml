<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActionBar">false</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="PrimaryTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:configChanges">orientation|screenSize</item>
        <item name="android:windowBackground">@color/cyanPrimary</item>
        <item name="android:statusBarColor">@color/cyanDark</item>
    </style>

</resources>
